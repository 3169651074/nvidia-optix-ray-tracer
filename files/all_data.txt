# vtk DataFile Version 2.0：    VTK 文件版本
Generated by NetDEM：           注释，说明生成程序
ASCII：                         数据是文本格式（而不是 BINARY）
DATASET POLYDATA：              数据集类型，这里是 POLYDATA，支持点、线、面等拓扑，
                                本文件只用到了点（POINTS + VERTICES）

POINTS：粒子中心坐标
POINTS 25 double：
有 25 个点
每个点 3 个 double 数（x, y, z）
紧接着的 25 行，每行 3 个数，对应粒子的位置坐标

VERTICES：把这些点作为独立的顶点单元
VERTICES 25 50：
有 25 个 vertex 元素
后面共有 50 个整数（计数 + 索引）
每一行 1 i 的含义：
第一个数字 1：这个 vertex 由 1 个点构成
第二个数字 i：引用的点的下标（从 0 开始，对应上面的 POINTS）
也就是说，这里把每个点都作为一个独立的“单元/粒子”来用，方便在 VTK 里进行可视化和附加属性。

POINT_DATA：与每个点对应的属性
说明接下来是点属性，数量为 25，与 POINTS 中的点一一对应，第 n 行的属性对应第 n 个点

下面依次是多组 SCALARS（标量字段）和 VECTORS（向量字段）
名称：id
类型：int
分量数：1（标量）
每行一个整数，对应点的 id（0 到 24）

标量：type
粒子类型标识（全部是 0，可能只有一种类型）

标量：damp_viscous
黏性阻尼系数，全部为 0

标量：damp_numerical
数值阻尼系数，全部为 0.5

标量：quat（四元数，写成 SCALARS 4 分量）
名称：quat
类型：double
分量数：4（其实就是一个 4 维向量：四元数 w,x,y,z 或 x,y,z,w，具体顺序要看 NetDEM 自己定义）
每行是 4 个 double，对应该粒子的朝向

VECTORS：速度 vel
类型：double，3 分量（x,y,z）
所有粒子的速度都是 (0, 0, -2)

VECTORS：自旋角速度 spin
角速度向量，全是 0

VECTORS：力 force
作用在每个粒子中心的力，全是 0

VECTORS：力矩 moment
力矩，全是 0

标量：shape_id
指示该粒子使用哪种形状（如球、椭球、多面体等），是一个整数索引
不同粒子有不同 shape_id

标量：size
粒子尺寸（半径或特征尺寸），这里都为 0.1

标量：mass
粒子质量，全部相同

VECTORS：转动惯量 moi
每个粒子的主惯量 (Ixx, Iyy, Izz)，有的各向同性（3 个相同），有的各向异性

标量：mask
掩码（一般用于筛选/启用/禁用粒子），这里全部为 0

从逻辑上看，你可以把每个点当作一个“粒子对象”，它包含：
几何：
position = (x, y, z) 来自 POINTS
标识：
id
type
shape_id
mask
动力学量：
vel = (vx, vy, vz)
spin = (ωx, ωy, ωz)
force = (Fx, Fy, Fz)
moment = (Mx, My, Mz)
物性：
size
mass
moi = (Ixx, Iyy, Izz)
姿态：
quat = (q0, q1, q2, q3)
所有字段都按 点索引 0~24 对齐，也就是说第 10 行的坐标、id、quat、vel、mass 等，都是同一个粒子的不同属性。

============================================

0："class": "trimesh_convex"

这是一个凸三角网格形状
关键字段：
"vertices"：一个很长的数组，每个元素是 [x, y, z]，就是网格的顶点坐标
"facets"：三元组列表，如 [2,0,8]，每个三元组就是一个三角形，索引的是 vertices 里的点
再加上 bound_aabb_max/min、vol、inertia 等物性和包围盒
1："class": "sphere"
球，不用显式网格，用半径等参数描述
2 ~ 7："class": "plane"

平面，给出中心 center、法向 dir_n、extent 等参数

对第 i 个粒子，读取 shape_id[i]
如果 shape_id == 0，就用 shape_0000000000.stl 这套三角形
如果 shape_id == 1，就用 shape_0000000001.stl 这套三角形
